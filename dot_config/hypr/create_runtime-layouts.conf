# Per-workspace layout rules. Generated by ~/.local/bin/hypr-layout-toggle.
# Sourced from hyprland.conf. Edit via Super+Alt+S, not by hand.
#
# `create_` chezmoi prefix means chezmoi apply only writes this file if
# it doesn't already exist — runtime edits (by the toggle script) survive.
