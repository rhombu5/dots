# PulseAudio clients (Chrome, Electron apps) ask for ~10ms interactive
# latency they don't need on a listening machine. Floor them at ~21ms so
# a starved client thread has real slack before the sink underruns.
pulse.properties = {
    pulse.min.req     = 1024/48000
    pulse.min.quantum = 1024/48000
}
