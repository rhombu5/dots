# zathura — PDF viewer. Colors live in `colors.zathurarc` (matugen-rendered);
# pulled in via `include` at the bottom of this file (zathurarc(5) supports
# `include <relative-path>`). This avoids the chezmoi/matugen write-stomp
# we'd get if both rendered to `zathurarc` directly.

set selection-clipboard          clipboard
set adjust-open                  best-fit
set pages-per-row                1
set scroll-page-aware            true
set scroll-step                  100
set guioptions                   ""
set window-title-basename        true
set window-title-page            true
set statusbar-basename           true
set statusbar-home-tilde         true
set incremental-search           true
set abort-clear-search           false
set sandbox                      strict
set font                         "JetBrainsMono Nerd Font 11"

# Modal-key conventions: vim-ish nav already default in zathura.
map [normal]    <C-h>  navigate previous
map [normal]    <C-l>  navigate next
map [normal]    R      reload
map [fullscreen] q     toggle_fullscreen

# Recolor toggle (hi/lo readability)
map r recolor

# Pull in the matugen-rendered color palette.
# Resolved relative to this file (zathurarc(5)).
include colors.zathurarc
